# ═══════════════════════════════════════════════════════
# JARVIS AI Second Brain — Python Dependencies
# ═══════════════════════════════════════════════════════

# ── Core Framework ──
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
pydantic-settings>=2.5.0

# ── LangGraph / LangChain ──
langgraph>=0.2.0

# ── Azure OpenAI ──
openai>=1.50.0

# ── Azure AI Search ──
azure-search-documents>=11.5.0
azure-core>=1.30.0

# ── Azure Blob Storage ──
azure-storage-blob>=12.22.0

# ── Azure Document Intelligence ──
azure-ai-documentintelligence>=1.0.0b3

# ── Database (async) ──
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.20.0          # SQLite async driver (local dev)
# asyncpg>=0.30.0          # PostgreSQL async driver (production — uncomment if needed)

# ── Authentication (JWT) ──
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# ── File handling ──
python-multipart>=0.0.9    # For FastAPI file uploads
PyMuPDF>=1.24.0            # PDF text extraction (fitz)
python-docx>=1.1.0         # DOCX text extraction
python-pptx>=1.0.0         # PPTX text extraction

# ── HTTP Client ──
httpx>=0.27.0

# ── Utilities ──
python-dotenv>=1.0.0
